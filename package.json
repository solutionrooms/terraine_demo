{
  "name": "terrain-gpu-grid",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "One-draw-call GPU tile grid — terrain foundation for a low-poly city builder (POC).",
  "scripts": {
    "serve": "python3 -m http.server 8080",
    "test": "node test/drawcall.test.mjs"
  },
  "devDependencies": {
    "three": "0.184.0"
  }
}
